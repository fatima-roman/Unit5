package domotica;

/**
 * Tipo enumerado.
 *
 * ¿Para qué sirve un enum?
 * - Para limitar una variable a un conjunto cerrado de valores.
 * - Evita usar cadenas de texto sueltas y reduce errores.
 * - Hace el código más seguro y expresivo.
 */
public enum ModoTermostato {
    CALEFACCION,
    REFRIGERACION
}